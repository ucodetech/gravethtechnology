deployment:
  tasks:
    # 1. Set paths based on your cPanel structure
    - export REPOPATH=/home/gravetht/repositories/gravethtechnology
    - export PUBLICPATH=/home/gravetht/public_html

    # 2. Sync the public directory contents to public_html (this includes the compiled build folder)
    - /bin/rsync -av $REPOPATH/public/ $PUBLICPATH/
